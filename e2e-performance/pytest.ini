[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    -v
    --tb=short
    --strict-markers

markers =
    baseline: Baseline performance tests (current implementation)
    optimized: Optimized performance tests (streaming + compression)
    slow: Tests that take a long time to run
